# CR0107-04F Migration Tracking Repair Plan — 执行计划（仅规划，不执行）

- **项目**: `D:\codex\cardrealm\card-app`
- **前置**: CR0107-04E（4 drift 已更名）、CR0107-04D-C（payment_refund 源已恢复）——远程 5 个孤儿 version 已闭合。
- **本任务目标**: 制定 `0001–0061` 进入 `schema_migrations` tracking 的 repair 执行计划。
- **性质**: 仅规划，**未执行任何命令**（未 repair / 未 db push / 未 deploy / 未 commit / 未 push）。
- **状态**: ✅ 计划已生成 — **WAITING CEO REVIEW**

---

## 1. 目标与现状

| 项 | 状态 |
|---|---|
| 本地编号迁移 | `supabase/migrations/00XX_*.sql` 共 **62 个文件**，版本 `0001`–`0061`，其中 **0046、0047 缺失**（62 = 61 个序号 − 2 缺失 + `0034_` 重复 3 个 = 59 个不同版本 + 3 个重复文件） |
| 远程 tracked | 仅 **5 条**（`20260801122630` / `20260801124633` / `20260801142033` / `20260802035915` / `20260802040518`）—— 与本地 62 个 `00XX` 版本 **零重叠** |
| 关键事实 | `supabase migration list --linked`（本任务只读核验）显示 5 条远程 version 的 **Local 列全空** → CLI **不识别 `00XX` 格式文件为迁移**（Supabase 要求 `YYYYMMDDHHMMSS_name.sql`） |

> **结论**：`0001–0061` 从未进入 tracking，且因格式不符，Supabase 工具链（含 `db push` / `migration list`）根本看不到它们。这正是 CR0107-04A「迁移失管」的根因之一。

---

## 2. 本地 0001–0061 文件清单与缺口

**需 repair 的版本（59 个，跳过缺失的 0046/0047）**：

```text
0001 0002 0003 0004 0005 0006 0007 0008 0009 0010
0011 0012 0013 0014 0015 0016 0017 0018 0019 0020
0021 0022 0023 0024 0025 0026 0027 0028 0029 0030
0031 0032 0033 0034 0035 0036 0037 0038 0039 0040
0041 0042 0043 0044 0045 0048 0049 0050 0051 0052
0053 0054 0055 0056 0057 0058 0059 0060 0061
```

**已知卫生问题（非本任务修复范围，但须在 repair 前知悉）**：
- **0046、0047 缺失**：无源文件 → 不 repair 这两个版本（避免 phantom 行）。
- **`0034_` 重复 4 个文件**：同一版本号 4 个文件（CR0107-04A 已记录）→ repair 只需对 `0034` 执行一次；重复文件须另行清理（建议归并/删除冗余，属独立卫生任务）。

---

## 3. 关键发现：`repair` 不执行 SQL（已验证）

依据 `supabase migration repair --help` 与官方文档（https://supabase.com/docs/reference/cli/supabase-migration-repair）：

- **描述**：`Repair the migration history table.` —— 仅操作 `supabase_migrations.schema_migrations` 历史表。
- **`--status applied`**：向历史表 **插入** 一条「已应用」记录（即使对应迁移实际未在远程跑过，仅补登）。
- **`--status reverted`**：从历史表 **删除** 该版本记录。
- **对比 `db push`**：后者才真正应用迁移文件并向历史表写记录；`repair` **不引用、不执行任何迁移文件内容**。
- **参数**：`version... string`（**可变参数**，可一次性传入多个版本）。

> ✅ 验证结论：`supabase migration repair --status applied` **绝不会执行迁移 SQL**，只写追踪行。Beta 已存在的对象不会被触碰、不会被重放。

---

## 4. 两条修复路径

### ⚠️ 核心约束（决定路径选择）

因本地文件为 `00XX` 格式（非 Supabase 时间戳格式），**CLI 不识别它们为迁移**。因此：

- 若直接 `repair --status applied 0001..0061`（数字版本）→ 会在 `schema_migrations` 插入 59 条 **数字版本行**，但 `migration list` 的 **Local 列仍为空白**（CLI 看不到本地 `00XX` 文件）→ tracking 记录与本地文件 **不关联**，验收（`migration list` 对齐）**无法达成**。这是一条「补了记录但不干净」的路径。

### 路径 A（最小：仅 repair 数字版本）—— 不推荐

```bash
supabase migration repair --status applied --linked \
  0001 0002 0003 0004 0005 0006 0007 0008 0009 0010 \
  0011 0012 0013 0014 0015 0016 0017 0018 0019 0020 \
  0021 0022 0023 0024 0025 0026 0027 0028 0029 0030 \
  0031 0032 0033 0034 0035 0036 0037 0038 0039 0040 \
  0041 0042 0043 0044 0045 0048 0049 0050 0051 0052 \
  0053 0054 0055 0056 0057 0058 0059 0060 0061
```

- ✅ 简单、快速，不碰文件。
- ❌ 验收不干净：`migration list` 出现 59 条 Remote=数字、Local=空 的孤儿行；与「本地文件进入 tracking」的目标有偏差。
- ❌ 若未来执行 `supabase db pull` / `db reset`，数字版本 phantom 行可能引发歧义。

### ✅ 路径 B（稳健：先重命名为时间戳 + repair）—— **推荐**

**原理**：先把 `00XX_name.sql` 重命名为合法时间戳格式 `202401010000XX_name.sql`（synthetic 但有效、单调递增、全部早于 2026-08-01 真实迁移），使 CLI 能识别；再 `repair --status applied` 标记这些 timestamp 版本为已应用 → `migration list` 的 Local 与 Remote 列均填充、完全对齐。

**时间戳映射方案（推荐）**：`00XX → 202401010000XX`
（例：`0001→20240101000001`，`0061→20240101000061`；全部位于 2024-01-01，严格递增，远早于远程真实迁移 `20260801…`，不会与任何现存 version 冲突，且早于待部署的 3 本地独有 + 03A-1。）

**前置步骤（B-1：重命名，需单独授权；本任务未执行）**：

```bash
# 在仓库根目录执行；0046/0047 无源文件跳过
for f in supabase/migrations/00[0-9][0-9]_*.sql; do
  base=$(basename "$f"); ver=${base%%_*}; suffix=${base#*_}
  [ "$ver" = '0046' ] && continue
  [ "$ver" = '0047' ] && continue
  newver="20240101${ver}"          # 00XX -> 202401010000XX
  git mv "$f" "supabase/migrations/${newver}_${suffix}"
done
# 注：0034_ 重复 4 文件会各自重命名为 20240101000034_xxx（同名 version 不同 suffix）
#     —— 须先归并重复文件，否则产生 4 个 version=20240101000034 的冲突文件。
```

> ⚠️ 重复 `0034_` 处理：重命名前须先决定保留哪一个 `0034` 文件（其余移至 `supabase/rollbacks/` 或删除），否则出现 4 个同 version 文件，`migration list` 会冲突。

**核心步骤（B-2：repair，需单独授权；本任务未执行）**：

```bash
# 1) 链接远程（不碰仓库 config：用临时 --workdir）
npx supabase link --project-ref xybpcsmjjcnkjwfsuder --workdir /tmp/sbverify_repair --yes

# 2) 批量 repair（可变参数，一次传入 59 个 synthetic timestamp 版本）
npx supabase migration repair --status applied --linked --workdir /tmp/sbverify_repair \
  20240101000001 20240101000002 20240101000003 20240101000004 20240101000005 \
  20240101000006 20240101000007 20240101000008 20240101000009 20240101000010 \
  20240101000011 20240101000012 20240101000013 20240101000014 20240101000015 \
  20240101000016 20240101000017 20240101000018 20240101000019 20240101000020 \
  20240101000021 20240101000022 20240101000023 20240101000024 20240101000025 \
  20240101000026 20240101000027 20240101000028 20240101000029 20240101000030 \
  20240101000031 20240101000032 20240101000033 20240101000034 20240101000035 \
  20240101000036 20240101000037 20240101000038 20240101000039 20240101000040 \
  20240101000041 20240101000042 20240101000043 20240101000044 20240101000045 \
  20240101000048 20240101000049 20240101000050 20240101000051 20240101000052 \
  20240101000053 20240101000054 20240101000055 20240101000056 20240101000057 \
  20240101000058 20240101000059 20240101000060 20240101000061
```

> 若担心单次 59 个版本过长，可按上述 10/批 拆分多次执行（结果等价）。`--workdir` 用后即删，不污染仓库 `config.toml`。

---

## 5. 完整命令列表（汇总）

| 步骤 | 命令 | 是否本任务执行 |
|---|---|---|
| 链接（临时 workdir） | `npx supabase link --project-ref xybpcsmjjcnkjwfsuder --workdir /tmp/sbverify_repair --yes` | 否（仅规划） |
| 重命名 62→59 文件 | 见 §4 路径 B-1 脚本 | 否（需单独授权） |
| 批量 repair（路径 A 数字版） | 见 §4 路径 A | 否 |
| 批量 repair（路径 B 时间戳版，推荐） | 见 §4 路径 B-2 | 否 |

> **强烈建议采用路径 B**：它是唯一能令 `migration list` 验收通过的方案。

---

## 6. 验证：repair 不会执行 SQL

- **help 文本**：`DESCRIPTION: Repair the migration history table.` —— 操作对象是历史表，非迁移文件。
- **官方文档**：`--status applied` = 向历史表**插入**记录；`--status reverted` = **删除**记录；全篇无任何「执行迁移脚本」描述。
- **逻辑**：`repair` 仅接收版本号字符串，不读取/不运行任何 `.sql` 文件内容。Beta 已有对象不受影响、不重放。
- ✅ 结论可靠：`migration repair` 是**纯元数据操作**，安全。

---

## 7. 执行后验收（`supabase migration list`）

**验收基准（路径 B 达成后）**：

```text
 Local           | Remote          | Time (UTC)
-----------------|-----------------|-----------------------
 20240101000001  | 20240101000001 | <inserted now>
 20240101000002  | 20240101000002 | <inserted now>
 ...             | ...             | ...
 20240101000061  | 20240101000061 | <inserted now>
 20260801122630  | 20260801122630 | 2026-08-01 12:26:30   (04E)
 20260801124633  | 20260801124633 | 2026-08-01 12:46:33   (04E)
 20260801142033  | 20260801142033 | 2026-08-01 14:20:33   (04D-C)
 20260802035915  | 20260802035915 | 2026-08-02 03:59:15   (04E)
 20260802040518  | 20260802040518 | 2026-08-02 04:05:18   (04E)
```

- ✅ **Local 列全部填充**（不再空白），Local 与 Remote 完全一致。
- ✅ 0001–0061（59 个）标记为 applied，与 Beta 实际对象态一致。
- ✅ 5 个真实 timestamp 孤儿 version 仍正确对齐（04E/04D-C 成果保留）。

**验收命令**：

```bash
npx supabase migration list --linked --workdir /tmp/sbverify_repair
```

> 若采用路径 A，则 `migration list` 会出现 59 条 `Remote=数字 / Local=空` 的脏行，**不通过该验收**。

---

## 8. 风险与注意事项

1. **`0034_` 重复 4 文件**：重命名（路径 B）前须先归并，否则生成 4 个同 version 冲突文件；repair（任一路经）对 `0034` 只跑一次即可。
2. **0046/0047 缺失**：无源文件，两版本**不 repair**（避免 phantom 记录）；若未来需追踪，须先补源文件。
3. **synthetic 时间戳顺序**：`20240101…` 全部早于真实迁移与待部署项，确保 `db push` 不会把它们当「新迁移」重放（repair 已标记 applied）。
4. **禁止 `db push`**：本计划与后续执行阶段均**禁止 `supabase db push`**（除非在完整 baseline→dry-run→deploy 流程中由单独授权任务执行）；repair 本身不触发 push。
5. **仓库 config 保护**：所有 CLI 调用使用 `--workdir /tmp/...` 临时目录，不改动仓库 `supabase/config.toml`。
6. **与 04E/04D-C 的协同**：本计划建立在 04E（4 drift 更名）+ 04D-C（payment_refund 恢复）已闭合 5 个远程孤儿 version 的基础上；repair 仅处理剩余 59 个 `00XX` 版本。

---

## 9. 约束遵守核对（本任务）

| 约束 | 遵守 |
|---|---|
| 仅规划 | ✅ 未执行任何 repair / push / deploy |
| 禁止 `migration repair` | ✅ 仅列命令，未运行 |
| 禁止 `db push` | ✅ |
| 禁止 `deploy` | ✅ |
| 禁止 `commit` | ✅ |
| 禁止 `push` | ✅ |
| 未修改数据库 | ✅（仅只读核验 `migration list`） |

---

## 10. 结论与建议

- **推荐路径 B**（重命名为 synthetic 时间戳 + `repair --status applied`）：唯一令 `supabase migration list` 验收通过的方案，使 59 个 `00XX` 版本真正「进入 tracking 且 Local/Remote 对齐」。
- **路径 A 仅作最小备选**，但验收不干净，不推荐。
- 执行前须先解决 `0034_` 重复与 `0046/0047` 缺失两件卫生问题。
- 全部步骤需另行授权（本任务仅产出计划）。

**状态：WAITING CEO REVIEW** —— 建议批准路径 B，并授权后续任务按顺序执行：① 归并 `0034_` 重复 → ② 重命名 59 文件为 `202401010000XX` → ③ `git add`+`commit` → ④ `migration repair --status applied`（59 个时间戳版本）→ ⑤ `supabase migration list` 验收。
